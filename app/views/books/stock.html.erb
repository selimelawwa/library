<h1> Books stock </h1>

<br>

<table class="table table-striped table-hover ">
  <thead>
      <tr>
        <th>Book Name</th>
        <th>Author</th>
        <th>Stock</th>
        <th>Ordered</th>
        
      </tr>
  </thead>
  <tbody>
  <% if not @books.nil?%>
  <% @books.each do |b| %>
  
    <% if b.quantity.to_i < 20 %>
     <tr class="red">
     <td><%=link_to "#{b.name.capitalize}" , book_path(b), class:"redlink"%></td>
    <% else %>
     <tr>
     <td><%=link_to "#{b.name.capitalize}" , book_path(b)%></td>
    <% end %>
    
    
    <td><%="#{b.author}"%></td>
    <td><%="#{b.quantity}"%></td>
    <td> <%= "#{b.ordered_times}"%> </td>
    <% if can? :destroy, b %>
              
           <td>  <%= link_to 'Edit', edit_book_path(b), class:"btn btn-xs btn-success active" %> |
             <%= link_to 'Destroy', book_path(b), class:"btn btn-xs btn-danger active",
                                method: :delete,data: { confirm: 'Are you sure?' } %> </td>
    <% end %>

     </tr>
  </tbody>
   <% end %>
 <% end %>
  </table>